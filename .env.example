# Human-readable model identifier (returned by /health)
MODEL_NAME=XGBoost

# Path to the serialized model file (joblib/pickle .pkl)
# Local:  relative path from the working directory inside the container
# GCS:    not supported natively — mount via Cloud Run volume or pre-copy at startup
MODEL_PATH=models/XGBoost_model.pkl
